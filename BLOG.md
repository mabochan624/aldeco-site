# ゲンバのはなし — 自動ブログの仕組み

アルデコのブログを「書く手間を減らしながら、質を落とさず、勝手に良くなっていく」形で運用するための仕組み。
静的HTML＋GitHub Pagesのまま、CMSなしで動く。

---

## 1. 北極星

> ★ **アルデコに出会う人を、増やし続ける。**
>
> **指標**: 検索とAIからサイトに来た人の数（月間クリック数）
> **ルール**: 目標値は置かない。**過去最高を超え続ける。**（成長の5か条④）

### なぜクリック数なのか

認知を測るなら「表示回数」でもいい。だがそれは**見られた止まり**で、選ばれていない。
クリックは「アルデコという名前を見て、選んだ」瞬間で、認知のなかで一番硬い。
対外的にも「月◯人がうちを見つけた」と言える。そして毎月必ず動くので、
「過去最高を、超える。」という運用がそのまま成立する。

### 星の下にある3層

| 層 | 中身 | 見る頻度 |
|---|---|---|
| **★北極星** | 検索・AIからのクリック数（月間） | 月1・過去最高更新だけを見る |
| **等級** | 指名検索数「アルデコ」 | 月1。増え始めたら**名前を覚えられた**段階の合図 |
| **診断** | 表示回数 / 記事→`/recruit/` 到達 / AI引用（5問中）/ 応募の一歩 | 月1。どこが効いたかの判別に使う |
| **入力** | 求職者向け一次情報記事の公開本数、記事あたりの `facts` 数 | 週1。**自分で動かせるのはここだけ** |

主読者は**求職者・若手**（18〜25歳、未経験・高卒含む）。
記事のネタもルールの追加も、星に効くかどうかで決める。
実体は `content/blog.rules.json` の `northStar` にあり、機械検証（求職者向け記事が過半を切ると警告）にも使われている。

### 計測タグの入れかた

`content/site.json` に2つの値を入れて `node tools/build.mjs` を実行するだけ。
全ページ（トップ・採用・プライバシー・ブログ記事）に自動で入る。個別のHTMLを触る必要はない。

- `ga4.measurementId` … GA4の管理 → データストリーム → 測定ID（`G-` で始まる）
- `searchConsole.verificationToken` … Search Console → 所有権の確認 → HTMLタグ の `content="..."` の中身だけ

GA4を入れると、次の3つが自動でイベントとして飛ぶ。

| イベント名 | 発火するとき |
|---|---|
| `recruit_click` | `/recruit/` または `#entry` へのリンクがクリックされた |
| `line_click` | LINE（`lin.ee` / `line.me`）へのリンクがクリックされた |
| `tel_click` | 電話番号がタップされた |

`from` パラメータにどのページから飛んだかが入るので、**どの記事が採用ページに送ったか**が分かる。

### 記録のしかた

```bash
node tools/metrics.mjs
```

```bash
node tools/metrics.mjs set 2026-08 clicks=12 brand=3 impressions=430 recruit=8 apply=0 ai=1
```

推移・過去最高・**連続更新月数**・次に超えるべき数字が出る。記録先は `content/metrics.json`。

`ai` は月1の手動チェック。`metrics.json` の `aiCheckQuestions` にある5問をAIに投げて、
アルデコの名前が出た数を数えるだけ。AIO対策が効いているかはこれでしか分からない。

### なぜ「一次情報」に全部を賭けるのか

検索でもAIでも、いま評価されるのは**そこにしか無い情報**だけ。
「外構工事とは」のような一般論はAIが自分で書ける。だが「500万円・50人工の現場を3人×2週間＝36人工でやり切った」は、
アルデコが書かないかぎり世界のどこにも存在しない。だから全記事に `facts`（数字か固有名詞を含む一次情報）を必須にしている。

---

## 2. ループの全体像

```
  ①ネタ投入（写真・チャットの一言・実話バンク）
        ↓
  ②生成       … Claude が blog-post スキルの取り決めに従って content/posts/*.md を書く
        ↓
  ③機械検証   … 保存と同時にフックが validate.mjs を実行
        ↓ 落ちたら②へ差し戻し（AIがその場で自己修復）
  ④ビルド     … build.mjs が記事HTML・一覧・RSS・sitemap・llms.txt・トップのNEWSを生成
        ↓
  ⑤人間ゲート … プレビューを見て代表がOKを出す
        ↓
  ⑥公開       … git push → GitHub Actions が再検証＋生成物のズレ検出 → Pages反映
        ↓
  ⑦計測       … 検索コンソール等の数字を見て、効いた型を見つける
        └────→ content/blog.rules.json を書き換えて①へ
```

**この仕組みの本体は ⑦→① の折り返し**。ルールを1か所（`blog.rules.json`）に置いてあるので、
そこを書き換えると「AIの書き方」と「機械の合否判定」が同時に変わる。運用しながらルールを厚くしていくと、
記事の質が勝手に上がっていく。

---

## 3. ファイル構成

| 場所 | 役割 |
|---|---|
| `content/posts/*.md` | **記事の正**。ここだけが唯一の情報源 |
| `content/blog.rules.json` | **取り決めの正**。北極星・スキーマ・禁止表現・文字数。ここを育てる |
| `content/site.json` | **計測タグの正**。GA4測定IDとSearch Console確認タグ。入れてビルドすれば全ページに入る |
| `content/metrics.json` | 北極星の記録簿。月1で数字を入れる |
| `content/_template.md` | 新規記事のひな形 |
| `tools/build.mjs` | 生成器。記事HTML・一覧・RSS・sitemap・robots・llms.txt・トップNEWS |
| `tools/validate.mjs` | 品質ゲート。エラー1件でexit 1 |
| `tools/hook-validate.mjs` | Claude Codeのフック本体（検証→合格なら自動ビルド） |
| `tools/preview.mjs` | CSS・画像を埋め込んだ1枚のプレビューHTMLを作る |
| `tools/photo.mjs` | 現場写真の取り込み。縮小＋**EXIF（GPS）完全削除** |
| `tools/photo.ps1` | Windowsの画像処理（.NET WIC・ASCIIのみ。※PS5.1はBOMなしの日本語.ps1を読めない） |
| `tools/photo.py` | Pillowでの画像処理（ImageMagickが無い環境の代替） |
| `tools/lib/jpeg.mjs` | 最後の砦。デコードせずJPEGの位置情報だけを落とす |
| `tools/ensure-image-tools.sh` | セッション開始時、画像ツールが無ければ入れる |
| `.claude/settings.json` | フックの設定（保存時の自動検証・起動時の画像ツール用意） |
| `content/inbox/` | 写真の受け取り箱 |
| `tools/lib/*.mjs` | Markdown・frontmatter・テンプレート（依存ゼロの自前実装） |
| `blog/` | **生成物。手で触らない**（次のビルドで消える） |
| `blog.css` | ブログ固有のデザイン。`style.css` を継承 |
| `.github/workflows/blog.yml` | push時に再検証し、生成物が記事とズレていないかを確かめる（書き込み権限なし） |
| `.claude/skills/blog-post/` | 記事を書くときの指針（会社のゴール直下） |
| `.claude/skills/genba-post/` | スマホから投稿するときの段取り（同上） |

npmパッケージは一切使っていない。`node tools/build.mjs` が動けばそれで完結する。

---

## 4. 使い方

### 記事を1本書く（AIに任せる）

Claude Codeで話しかけるだけ。

```
今日は金ヶ崎でブロック積み。雨で段取り変えた。ブログにして
```

`blog-post` スキルが起動して、取り決めどおりの記事を書く。保存した瞬間にフックが検証を回し、
落ちたらAIが自分で直す。通ったら自動でビルドされる。

### スマホから投げる（`genba-post` スキル）

写真を撮って、一言送るだけ。

```
今日は金ヶ崎でブロック積み。雨で段取り変えた
```

写真の候補が出るので番号で選ぶ → 足りない事実を最大3つだけ聞かれる → 記事が出来上がる →
プレビューを見て「OK」で公開。ネタに困ったら「ネタある?」と聞けば、最近の写真を見て記事案を3つ返す。

### 声をかけてもらう（`genba-blog-nudge`）

**平日12時半ごろ（昼休憩）**に自動タスクが走る。やることは通知ではなく、下ごしらえ。

1. 最後の投稿から何日空いたかを数える
2. **前回投稿から3日以内なら、黙って終了する**（毎日走るので、用がない日に黙れないと雑音になる）
3. 3日以上空いていたら「今日の現場の写真と一言、送れそうですか？」と聞く
4. `content/inbox/` に写真が来ていれば拾って、記事案を最大3つ出す

**写真は自動では取りに行かない。**送ってもらう前提で動く。返事は番号だけでいいし、「今日はパス」でも食い下がらない。

写真は**チャットに添付するだけ**でいい。添付は `~/.claude/uploads/<セッションID>/` に保存されるので、

```bash
node tools/photo.mjs recent
```

で見つかる（直近30分の添付だけを対象にする）。スマホに新しいアプリを入れる必要はない。

タスクの中身は `~/.claude/scheduled-tasks/genba-blog-nudge/SKILL.md`。
**このアプリが開いているときに動く。**閉じていた場合は次に開いたときに走るので、
確実な引き金が欲しければスマホ側の繰り返しリマインダーを併用する。

### スマホだけで完結させる（PCを開かない）

スマホのブラウザで **claude.ai/code** を開き、GitHubの `aldeco-site` リポジトリで作業する。
PCの電源は関係ない。スキルもツールもリポジトリの中にあるので、そのまま同じ取り決めで動く。

```
今日は◯◯やった
```
と書いて写真を添付 → 記事ができる → プレビューを見て「OK」 → 公開。

クラウドのコンテナは毎回捨てられるので、写真を縮小する道具が入っていないことがある。
セッション開始時に `tools/ensure-image-tools.sh` が走り、**何も無ければ ImageMagick を入れる**。
すでに使えるもの（ImageMagick / Pillow / ffmpeg）があれば何もしないので、起動が遅くなるのは初回だけ。

**環境の違いは `node tools/photo.mjs doctor` で分かる。**

| 環境 | 写真の処理 |
|---|---|
| 代表のPC（Windows） | 縮小・回転補正・メタデータ削除すべて可。JPEG/PNG/HEIC |
| クラウド（ImageMagickあり） | 同上 |
| クラウド（何も無い） | **位置情報の削除のみ。縮小・回転は不可。JPEGのみ** |

どの環境でも**位置情報だけは必ず消える**。消せない形式（HEIC等）は、処理せずエラーで止まる。
「消せないまま公開する」経路は存在しない。

### 写真の扱い（重要）

**生のスマホ写真をそのまま `img/` に置いてはいけない。** EXIFに GPS 座標＝お客様の家の位置が入っている。

```bash
node tools/photo.mjs list
```

```bash
node tools/photo.mjs add my-slug "C:/path/to/photo.jpg"
```

`add` は 長辺1600pxに縮小 → JPEG再エンコード → `img/blog/<slug>-N.jpg` に保存する。
再エンコードで**EXIFは完全に消える**（GPS文字列を仕込んだ画像で消滅を確認済み）。向きの補正も入る。

取り込み元は `blog.rules.json` の `photos.sources`。既定では `content/inbox/` を見る。

**写真はチャットに添付するだけでいい。** 添付は `~/.claude/uploads/<セッションID>/` に保存されるので、
`node tools/photo.mjs recent` で拾える。スマホに追加のアプリを入れる必要はない。
PCで作業するときは `content/inbox/` に置いてもよい。

### 手で書く

```bash
cp content/_template.md content/posts/2026-08-20-my-slug.md
```

書いたら:

```bash
node tools/validate.mjs
```

```bash
node tools/build.mjs
```

### プレビューを見る

```bash
node tools/preview.mjs kanegasaki-50ninku
```

CSSと画像を埋め込んだ1枚のHTMLができるので、そのまま開くかスマホに送って確認できる。

### 公開する

```bash
git add -A && git commit -m "post: 記事タイトル" && git push
```

数分で https://aldeco.co.jp/blog/ に反映される。

---

## 5. SEO / AIO 対策の中身

ビルダーが自動で入れているもの:

- **JSON-LD**: `Organization` / `BlogPosting`（著者・発行者・一次情報を `about` に列挙）/ `BreadcrumbList` / `FAQPage`
- **canonical・OGP・Twitter Card**: 全ページ
- **sitemap.xml / robots.txt / RSS**: 記事追加で自動更新
- **llms.txt**: AI向けの会社要約インデックス。理念・採用条件・記事一覧・全記事の一次情報を1ファイルに集約し、引用を許可する明文を置いている
- **要点ブロック**: 記事冒頭の3行。AIが最初に読む場所
- **一次データブロック**: 記事末尾。数字だけを構造化して並べる＝引用されやすい形

検証が強制しているもの:

- タイトル16〜42字、description 60〜120字（検索結果で切れない）
- 本文800字以上、`##`見出し2つ以上（薄い記事とAIが構造を読めない記事を弾く）
- サイト内リンク1本以上、リンク切れ検出、画像alt必須
- 誇大表現の禁止（「業界No.1」「必ず稼げる」等）
- 借り物構文の警告（「地域に根ざした」「アットホームな職場」等）
- `facts` に数字か固有名詞が無ければ**不合格**

---

## 6. これから育てるところ（未実装）

| 段階 | 内容 | ブロッカー |
|---|---|---|
| ~~計測を入れる~~ **完了** | Search Console（所有権確認・サイトマップ送信）、GA4（測定ID `G-1KLHYNGL5F`）とも稼働中 | — |
| **数字をループに戻す** | 記事ごとの順位・クリックを見て、効いた型を `blog.rules.json` に反映する | 3ヶ月ぶんのデータ |
| **GA4のキーイベント指定** ←次 | `recruit_click` 等を重要指標として扱わせる | イベントが管理画面の一覧に出るまで待つ |
| **定期実行の強化** | 平日昼のリマインドは稼働中。将来は写真の候補出しまで自動化する | — |
| **JobPosting構造化データ** | 求人条件が確定したら `/recruit/` に付ける。Googleしごと検索に載る | 試用期間・勤務時間・保険の確定 |

### 写真の自動取得について（現状の結論）

**「スマホの写真を勝手に見て候補を出す」は、いまはやらない。**

- **Googleフォト**: 不可。Google が2025年3月に Photos Library API のライブラリ読み取り権限を廃止し、
  いまは Photos Picker（毎回ユーザーが選ぶ）かアプリ自作メディアしか触れない。
- **OneDrive / Google Drive**: 技術的には可能だが、**スマホ側にアプリを入れる必要がある**。
  そこまでして自動化する価値が、今の投稿頻度（月4本）に見合わない。

代わりに採ったのが**プッシュ型**。昼にこちらから声をかけて、そのとき写真をチャットに添付してもらう。
本人の実感としても「撮った瞬間より、撮ったあとに聞かれる方が続く」。

添付された写真は `~/.claude/uploads/` に落ちるので、`node tools/photo.mjs recent` でそのまま拾える。
**実測**: 3.3MB・4032×3024・EXIF 22タグの写真が、301KB・1600×1200・EXIF 2タグ（量子化テーブルのみ）になり、
Orientation=3 の回転補正も正しく効いた。追加のアプリもAPIも要らない。

将来スマホ→PCの同期フォルダができたら、`blog.rules.json` の `photos.sources` に
そのパスを1行足すだけで自動取得に切り替わる。仕組み側は既に対応済み。

### 計測が入るまでの暫定ルール

星の数字が取れない間は、**入力指標だけを守る**。「求職者向けの一次情報記事を、月4本」。
ネタが尽きたら質を下げるのではなく、本数を落とす。薄い記事は星に対してマイナスになる。
